/**
 * Demo store data re-exported from `components/stores/data.ts`.
 * This file serves as the canonical demo data location.
 * The original `components/stores/data.ts` is preserved for
 * backward compatibility and re-exports from here.
 *
 * Replace with real API data when the backend ships.
 */

export { STORES, CATEGORIES } from '../../components/stores/data';
