import { CheckoutScreen } from '../components/checkout/CheckoutScreen';

/**
 * Expo Router flat route for `/checkout`. Thin wrapper — all of
 * the composition lives in `components/checkout/CheckoutScreen.tsx`
 * so this file stays a single-line re-export.
 */
export default CheckoutScreen;
