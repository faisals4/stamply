import { CartScreen } from '../components/cart/CartScreen';

/**
 * Expo Router flat route for `/cart`. Thin wrapper — all of the
 * logic lives in `components/cart/CartScreen.tsx` so this file
 * stays a single-line import.
 */
export default CartScreen;
