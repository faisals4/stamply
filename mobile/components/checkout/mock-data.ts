/**
 * Flat delivery fee in SAR, added to the order total in the
 * summary block. Real orders will compute this from distance +
 * merchant rules via the backend; for now it's a constant.
 */
export const DELIVERY_FEE = 15;
